export type Themes = "dark" | "light"

export type Variants = "primary" | "secondary" | "white" | "text"

export enum Sizes {
    "xs" = 11,
    "sm" = 14,
    "base" = 16,
    "lg" = 18,
    "xl" = 20,
    "2xl" = 24,
    "3xl" = 28,
}
